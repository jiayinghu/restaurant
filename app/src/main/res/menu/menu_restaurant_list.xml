<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".RestaurantListActivity">

    <!-- Refresh, should appear as action button -->
    <item android:id="@+id/action_refresh"
          android:title="@string/action_refresh"
          android:icon="@drawable/ic_action_refresh"
          android:showAsAction="ifRoom" />

    <!-- Go to the map view, should appear as action button -->
    <item android:id="@+id/action_location"
          android:title="@string/action_location"
          android:icon="@drawable/ic_action_place"
          android:showAsAction="ifRoom" />

    <item android:id="@+id/action_recommend"
        android:title="Recommend"
        android:icon="@drawable/ic_action_question_mark"
        android:showAsAction="ifRoom" />
</menu>
